version: 2

models:
  - name : CDC_PLANS
    config:
      materialized: table
    description : SCD 2 Table for plans
    columns:
      - name : id
        data_tests :
          - not_null
          - unique
